Presupuesto = 3650
acumulado = 0
productos = [
  [laptop, 1200], 
  [smartphone, 700],
  [chaquetas, 120],
  [jeans, 50], 
  [manzanas, 2]
  ]
k = 0
n = len(productos)
combinacionDeProductos(productos [], carrito [], k, n, presupuesto, acumulado){

  #Validar que se acabaron los productos
  if (n == k) {
		return
  }
  else{
    while (k < n && acumulado <= presupuesto) {
      objetoActual = productos[k][0]
      precioActual = productos[k][1]

      if (acumulado + precioActual <= presupuesto){
        #Llamado recursivo
	      carrito.append(objectoActual)
        acumulado += precioActual
        combinacionDeProductos(productos[], carrito[], k, n, presupuesto, acumulado)
        
      }
      #llamado recursivo 
     else {
        combinacionDeProductos(productos[], carrito[], k+1, n, presupuesto, acumulado)
      }
    }
    print(carrito)
    print(restante)
  }
}

